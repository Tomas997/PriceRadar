spring:
  application:
    name: api-gateway

server:
  port: 8080

gateway:
  parser-url: ${PARSER_SERVICE_URL:http://localhost:8082}
  product-url: ${PRODUCT_SERVICE_URL:http://localhost:8081}

logging:
  level:
    org.example.apigateway: DEBUG